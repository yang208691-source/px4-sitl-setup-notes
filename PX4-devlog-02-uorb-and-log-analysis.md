# PX4 二次开发日志 02：uORB 采样实验、工具链踩坑，与一次完整的日志排故

> **日期**：2026-09-26
> **环境**：Windows 11 + WSL2 Ubuntu 24.04 + PX4 **v1.15.4** + Gazebo Harmonic 8.15.0
> **前置**：[`PX4-devlog-01-first-module.md`](./PX4-devlog-01-first-module.md)
> **结果**：搞懂 uORB 的采样语义，跑通任务飞行，完成一次"从现象到真相"的完整排故
> **性质**：本文记录 **5 个工具链坑** + uORB 采样实验 + 一次完整的日志排故复盘 + 方法论

---

## 关于这份记录

**作者是飞控领域的初学者** —— 刚毕业入职，从零开始学 PX4 二次开发，没有师兄带，也没有既有技术积累可接手。

**这不是一份权威教程，而是一个新人边踩坑边学习的真实过程。**

读的时候请注意：

| 标记 | 含义 |
|---|---|
| ✅ | **实测数据和复现步骤是可靠的** —— 每条都实际跑过，有终端输出佐证 |
| ⚠️ | **原理性解释可能有理解偏差** —— 我尽力了，但受限于当前水平，欢迎指正 |
| 📌 | **版本敏感** —— 所有内容基于 PX4 **v1.15.4**，其他版本可能不同 |

**如果发现错误，欢迎提 Issue 或 PR** —— 这对我是很有价值的反馈。

---

## 本篇的定位

**日志 01** 解决"**怎么改 PX4**"——模块机制、构建体系、观测能力。

**本篇**解决"**怎么验证自己的理解**"——设计实验、判读数据、识别误判。

**核心不是学到了什么知识，而是走完了几轮"假设 → 验证 → 修正"的循环。**

---

# 一、uORB 采样行为：一次完整的实验

## 1.1 起点：一个简单的疑问

`uORB` 的发布/订阅，到底是"消息队列"还是"共享最新值"？

**用 `px4_hello` 做了四轮实验。**

## 1.2 实验一：`copy()` 到底返回什么

**改法**：把循环周期从 500 ms 改成 1 ms，打印 `copy()` 的返回值。

**结果**：

```
[ 0] NEW  ts=29200000
[ 1] NEW  ts=29200000    ← 时间戳相同！读到了重复数据
[ 2] NEW  ts=29204000
...
20 次全部返回 true
```

**发现**：

> **`uORB::Subscription::copy()` 只要话题存在就返回 `true`，不判断数据是不是新的。**
>
> **要判断"有没有新数据"，必须用 `updated()`。**

**这个结论是从实测推翻了"想当然"得到的**（我原本以为 `copy()` 会检查）。

## 1.3 实验二：循环周期由什么决定

**改法**：`px4_usleep(5000)`，并记录每次循环的实际耗时。

**结果**：

```
[ 1] dt= 8000 us
[ 2] dt= 8000 us
...
[19] dt= 8000 us
```

**发现**：

```
实际循环周期 = px4_usleep(5000) + PX4_INFO 打印开销
             = 5 ms + 约 3 ms
             = 8 ms
```

> **`PX4_INFO` 打印一次约 3 ms。** 调试打印会显著改变时序——这在真机上可能是致命的（控制周期被拉长 → 相位裕度腰斩）。

**这解释了为什么 PX4 有 `PX4_DEBUG` 宏**（只在 debug 构建里编译进去）。

## 1.4 实验三：相位错位

把 `px4_usleep` 改成 1000（循环周期约 4 ms），和话题的 4 ms 周期相同。

**结果**：20 次循环里有 1 次读到重复数据。

**根因**：**两个频率相同的独立系统，相位会缓慢漂移。**

```
话题更新：  |----4ms----|----4ms----|----4ms----|
你的读取：  |---4ms---|---4ms---|---4ms---|
                       ↑
                 偶尔踩在更新之前 → 读到上一份
```

> **这个现象在控制工程里叫"采样抖动（jitter）"。**
> **PX4 在真机上用 IMU 中断驱动控制循环，就是为了让控制器和传感器锁在同一个时钟上。**

## 1.5 实验四：平均值陷阱

用 `analyze_rate.py` 分析日志里 `vehicle_attitude` 的周期分布：

```
样本数: 147034
平均周期: 4000.0 us  →  250.00 Hz
最小 / 最大: 0 / 8000 us

周期分布:
   4000 us : 147030 次 (100.00%)
```

**平均 250 Hz，但分布里藏着 3 个异常**：

```
第 134600 个: ts 540816000 -> 540824000   dt=8000   ← 掉一拍
第 134602 个: ts 540828000 -> 540836000   dt=8000   ← 掉一拍
第 134603 个: ts 540836000 -> 540836000   dt=0      ← 重复
```

**进一步排查**：

| 假设 | 验证 | 结果 |
|---|---|---|
| EKF 因为执行时间而掉拍？ | 周期分布 100% 是 4000 us | ❌ 推翻 |
| 四元数复位导致？ | `quat_reset_counter` 只变了 1 次（t=2.4s） | ❌ 推翻（异常在 540s） |
| **logger 采样错位？** | 出现"重复时间戳"（EKF 自己不会发重复） | ✅ **最可能** |

**结论**：**数据源（EKF）是干净的，异常出在记录层（logger 的采样）。**

> **这次实验最大的价值不是找到答案，而是"排除掉一个方向"。**
> 在它之前，嫌疑有两个（EKF 掉拍 / 读取拿到旧值）；之后只剩一个。

## 1.6 uORB 采样的完整心智模型

```
发布者（如 ekf2，250 Hz）
    │  写入一份"最新值"
    ▼
┌─────────────────┐
│  话题的最新数据   │  ← 所有订阅者读的都是这里
└─────────────────┘
    ↑        ↑
订阅者 A   订阅者 B     ← 互不影响，永不阻塞发布者
```

**四条推论**：

| 推论 | 影响 |
|---|---|
| 读到的是**最新值**，不是"每一条都收到" | 慢的订阅者会丢帧 |
| **发布者永不被阻塞** | 这是实时性的关键保证 |
| 默认队列深度 = 1 | `uorb top` 里 `#Q` 大多是 1 |
| **要判断"新数据"必须用 `updated()`** | `copy()` 只保证"有数据"，不保证"是新的" |

---

# 二、Python 工具链的 5 个坑

## 坑 1：PEP 668 —— 系统 Python 被保护

**现象**：

```
error: externally-managed-environment
× This environment is externally managed
```

**根因**：Ubuntu 23.04+ 引入 **PEP 668**，把系统 Python 标记为"外部管理"。

**为什么**：系统里很多工具（包括 `apt` 自己）依赖 Python。pip 装了不兼容的版本，**可能把系统工具搞崩**。

**解法**：**用 venv**。

```bash
python3 -m venv ~/PX4-notes/venv
~/PX4-notes/venv/bin/pip install <包名>
~/PX4-notes/venv/bin/python script.py      # 不需要 activate
```

> **venv 的"进出"不是必须的**——直接用全路径调用解释器，永远不会搞混"我在哪个环境里"。

## 坑 2：`Network is unreachable` 不是"连不上"

**现象**：

```
Failed to establish a new connection: [Errno 101] Network is unreachable
```

**关键判读**：

| 错误 | 含义 | 常见原因 |
|---|---|---|
| `Connection refused` | 能到达目标，但被拒绝 | 代理没开 |
| `Connection timed out` | 能到达，但没响应 | 防火墙、丢包 |
| **`Network is unreachable`** | **内核说：根本没有路由** | **根本没网** |

**换源、换协议都没用——问题在网络层，不在应用层。**

**解法（网络恢复后）**：用国内镜像源，绕开代理。

```bash
pip config set global.index-url https://pypi.tuna.tsinghua.edu.cn/simple
```

## 坑 3：MAVSDK v4 的破坏性变更

**现象**：

```
TypeError: System is not constructed directly, get it from Mavsdk instead.
Since v4, the 'mavsdk' package is the native MAVSDK binding, which has a different API
from the gRPC-based MAVSDK-Python (mavsdk<4).
```

**根因**：

| 版本 | 是什么 | API |
|---|---|---|
| `mavsdk < 4` | gRPC 封装的 Python 包 | `System()` / `drone.action.arm()` |
| `mavsdk >= 4` | **原生绑定**（C++ 直接绑到 Python） | 完全不同 |

**解法**：`pip install 'mavsdk<4'`（注意引号，`<` 在 shell 里有特殊含义）

> **⚠️ 教训：官方最新版 ≠ 你应该用的版本**
>
> | 场景 | 踩过的坑 |
> |---|---|
> | PX4 固件 | main 分支的 `gz_bridge` 静默失效 → 换 v1.15.4 稳定版 |
> | MAVSDK | v4 换了 API，v3 的脚本全废 → pin 到 `<4` |
>
> **同一个教训，换了战场。工程上默认"稳定优先"，不是"最新优先"。**

## 坑 4：MAVSDK 是"Python 壳 + C++ 内核"

**现象**：脚本卡死在这里，没有任何有意义的报错。

```
drone.connect(system_address="udpin://:14540")
  → _init_plugins()
    → AsyncPluginManager.create()      ← 创建 gRPC 后端管理器
      → channel_ready()                ← 卡住
```

**根因**：**MAVSDK-Python 不是纯 Python 库。**

```
你的 Python 脚本
      ↓  gRPC（localhost:50051）
  mavsdk_server          ← C++ 二进制，MAVSDK-Python 自带
      ↓  UDP
   PX4 SITL
```

**它启动一个 C++ 子进程，通过 gRPC 通信。** 如果子进程没起来（或端口/IPv6 解析有问题），Python 端就卡在 `channel_ready`，**而且报错完全指向不到真正的原因**。

**解法**：分层验证——手动启动 `mavsdk_server`，把"自动启动"这个黑盒拆开。

```bash
SERVER=~/PX4-notes/venv/lib/python3.12/site-packages/mavsdk/bin/mavsdk_server
$SERVER -p 50051 udpin://0.0.0.0:14540
```

> **⚠️ 教训：多一层进程 = 多一类坑。**
> 这种"Python 壳 + 二进制内核"的架构在 AI/仿真工具里很常见，**它天生脆弱**。真机项目里，纯 Python 方案（如 `pymavlink`）虽然 API 更底层，但少一层就少一类问题。

## 坑 5：matplotlib 中文显示成方框

**现象**：图标题变成 `□□□□`。

**根因**：matplotlib 默认字体不含中文字形。

**解法**：

**方案 A（装字体）**——在 **WSL 终端**里：

```bash
sudo apt install fonts-noto-cjk
fc-list :lang=zh | head -5          # 验证
```

然后在 Python 脚本里：

```python
plt.rcParams['font.sans-serif'] = ['Noto Sans CJK JP', 'DejaVu Sans']
plt.rcParams['axes.unicode_minus'] = False
```

**方案 B（推荐，改用英文标签）**：

| | 中文标签 | 英文标签 |
|---|---|---|
| 依赖字体 | ✅ 需要装 + 配置 | ❌ 不需要 |
| 换机器/换环境 | 可能又变方框 | 永远正常 |
| 专业图表惯例 | 少 | **多** |

**本仓库所有绘图脚本统一使用英文标签。**

---

# 三、一次完整的日志排故："方波"之谜

## 3.1 现象

用 QGC 做了一次**航迹规划 + 返航**飞行，日志分析后发现：

**水平速度曲线呈现"方波"** —— 在 0 和 5 m/s 之间垂直跳变。

## 3.2 逐层排除

### 第一步：记录层有问题吗？

```python
dt = np.diff(timestamp)
```

```
平均 8000 us   最小 0   最大 16000
   8000 us : 91547 (99.99%)
```

**99.99% 的间隔是 8000 us**，和 `vehicle_local_position` 的 125 Hz 完全吻合。

**→ 记录层正常，排除。**

### 第二步：速度估计有问题吗？

```python
a = np.diff(vh) / np.diff(t)
```

```
EKF 速度 最大加速度: 4.1 m/s²     |a|>10 的样本 1
```

**物理上限**：

```
a_max = g × tan(θ_max) = 9.8 × tan(18°) ≈ 3.2 m/s²
```

**实测 4.1 m/s² 与上限同量级**，且剧烈异常只有 1 个样本。

**→ 速度估计正常，排除。**

### 第三步：放大时间尺度

```python
# 取速度跳变前后各 10 秒，单独画图
```

**结果**：**平滑的 S 形加速曲线**，从 575 s 到 577.5 s 速度从 0 升到 5 m/s。

## 3.3 真因：**时间尺度错觉**

```
加速时间 1.2 ~ 2.5 秒
图的时间跨度 732 秒
占比 0.17% ~ 0.34%
子图宽度约 500 像素 → 过渡段不到 2 像素
```

**"垂直的上升沿"，实际是一条平滑曲线——被时间轴压缩成了一个像素。**

> **这是我在这次排故中犯的核心错误**：没做时间尺度换算，就下了"物理上不可能"的结论。
>
> **数据从来没错，是看图的人错了。**

## 3.4 但数据里其实有**真东西**

放大之后，这次飞行的控制品质反而看得更清楚了：

| 观察 | 含义 |
|---|---|
| **加速时间 2.5 s，加速度 2 m/s²** | 低于物理上限 3.2 m/s² → **控制器主动限制了加速度** |
| **曲线是 S 形**（不是梯形） | **限 jerk（加加速度）的轨迹生成**，响应更柔和 |
| **超调仅 1%**（5.05 → 5.0） | 位置环阻尼调得很干净 |
| **最大速度 5.05 m/s** | 正好是 `MPC_XY_VEL_MAX` 默认值 5 m/s |
| **返航爬升到 32 m** | `RTL_RETURN_ALT` 的避障爬升逻辑 |
| **`xy_valid` 99.56%** | 位置数据可信，不是 EKF 漂移 |

---

# 四、方法论沉淀

**这一节和飞控无关，但比任何具体命令都值钱。**

## 4.1 看到"陡变"，先算三个数

**这是本篇最重要的一条。**

```
① 这个变化用了多长时间？      ← 从数据算，不是从图看
② 这个变化率物理上可能吗？    ← 对比物理上限
③ 图的时间尺度是多少？        ← 1 秒在图上占几个像素？
```

**我这次跳过 ① 和 ③ 就下结论，结果判错了。**

**任何人看长时间跨度的图都会犯这个错。** 立成纪律，不靠记性。

## 4.2 物理约束是最硬的判据

**判断数据真假，先问"物理上可能吗"：**

| 物理量 | 约束 | 怎么算 |
|---|---|---|
| 加速度 | `a ≤ g·tan(θ_max)` | 多旋翼靠倾斜产生水平力 |
| 角速率 | 受电机响应和结构强度限制 | 查机型规格 |
| 高度变化率 | 受推力上限限制 | `(T/W - 1)·g` |

**数据超出物理上限 → 一定是估计层或记录层的问题，不是物理世界的问题。**

## 4.3 排除法占了排故的 80%

**这次排故的四步里，三步是排除**：

```
记录层  → 排除
估计层  → 排除
时间尺度 → 找到真相
```

**"找到真因"只占一小部分时间。别指望灵光一闪，要靠系统性排除。**

## 4.4 验证方法本身也要被验证

**我在同一次会话里，连续两次用错关键词做 `grep` 校验**：

| 我 grep 的 | 结果 | 真相 |
|---|---|---|
| `"不是权威教程"` | README ✓ / 另两份 ✗ | 另两份写的是"这不是**一份**权威教程" |
| `"初学者"` | 另两份 ✓ / README ✗ | README 用的是"飞控新人" |

**两次都是假阴性——文件其实都改对了，是校验方法有问题。**

> **排故时，"验证结果"和"验证方法是否可靠"同等重要。**

## 4.5 平均值 ≠ 分布

| 场景 | 平均值 | 分布揭示的 |
|---|---|---|
| EKF 发布周期 | 250.00 Hz | 有 3 个异常间隔 |
| 循环耗时 | 8000 us | 首次循环偏短 |

**`uorb top` 给你平均值，`analyze_rate.py` 给你分布。两者结合才能判断时序健康。**

## 4.6 分层排查：从下往上，不跳步

**网络排查的层次**：

```
物理层  手机 USB 插着吗？共享开着吗？
   ↓
链路层  WSL 有 IP 吗？有默认路由吗？
   ↓
网络层  能 ping 通网关吗？能 ping 通外网 IP 吗？
   ↓
应用层  DNS 能解析吗？HTTPS 能握手吗？
   ↓
最后才是 pip
```

**直接盯着最后一层的报错查，你永远在打转。**

**这和 setup-log 里的"Gazebo topic 三层诊断法"是同一个方法论。**

## 4.7 工具选型：够用就好，别恋战

**MAVSDK 折腾未果后，转用 QGC，立刻拿到结果。**

| | MAVSDK | QGC |
|---|---|---|
| 能力 | 程序化、可重复、可自动化 | 手动、直观、上手快 |
| 依赖 | Python + gRPC + C++ 二进制 | 一个安装包 |
| 适合 | 批量实验、参数扫描 | **看现象、理解系统** |

**当前阶段的目标是"理解系统"，QGC 完全够用。**

> **工程判断：调试有价值，但要有止损线。** 一个工具卡住超过合理时间，换条路走——**"理解现象"比"用对工具"更重要。**

---

# 五、当前进度与下一步

## 已完成

- [x] 搞懂 uORB 的采样语义（最新值模型、`updated()` vs `copy()`）
- [x] 理解采样抖动、相位错位、平均值陷阱
- [x] 打通 Python 工具链（venv、镜像源、版本 pin）
- [x] 用 QGC 完成航迹规划 + 返航，拿到完整任务飞行日志
- [x] 完成一次"现象 → 假设 → 排除 → 真相"的完整排故

## 未完成

- [ ] **最低驱动认知清单**（传感器链路 / 执行器链路 / 从日志判断硬件 vs 算法）
- [ ] **精读 `mc_att_control` / `mc_pos_control`**
- [ ] MAVSDK 的 gRPC 连通性问题（暂缓，QGC 已够用）

## 下一步

```
带着"为什么加速曲线是 S 形而不是梯形"这个问题，
去读 mc_pos_control。
```

**手上有热的、具体的现象，读代码的效率完全不同。**

---

## 一句话总结

> **这一天的核心不是学到了什么知识，而是走完了几轮"假设 → 验证 → 修正"的循环。**
>
> **而且验证了三次"我原本以为对的"是错的**：
> `copy()` 不检查新数据、循环周期由打印决定、"方波"是错觉。
>
> **判断力的生长方式，就是让"我以为"不断被数据打脸。**

---

## 关联文档

- [`PX4-devlog-01-first-module.md`](./PX4-devlog-01-first-module.md) —— 自定义模块、观测能力、控制链路频率地图
- [`PX4-SITL-setup-log.md`](./PX4-SITL-setup-log.md) —— 环境搭建，13 个坑
- `plot_mission.py` —— 任务飞行分析（轨迹/高度/速度/姿态/推力）
- `analyze_rate.py` —— 话题发布周期分布分析
- `plot_attitude.py` —— 姿态与角速率曲线

## 许可

随意取用、修改、转发。
